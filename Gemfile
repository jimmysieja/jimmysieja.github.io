source "https://rubygems.org"

# GitHub Pages builds this site with its own pinned toolchain; the gem below
# keeps a local `bundle exec jekyll serve` matching what GitHub actually runs.
gem "github-pages", group: :jekyll_plugins

# Local preview server for Jekyll 4 / Ruby 3+
gem "webrick", "~> 1.8"
